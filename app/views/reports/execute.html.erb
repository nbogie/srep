<h1>Results of report  "<%= @report.title %>"</h1>
This is a preview.  If you want to manipulate it in a spreadsheet, get 
<%= link_to 'the raw csv', execute_report_path(@report, :format => :csv) %> instead.

<pre><%= @csv_result %></pre>

<%= link_to 'Execute to csv', execute_report_path(@report, :format => :csv) %> | 
<%= link_to 'Execute to html', execute_report_path(@report, :format => :html) %> |
<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Show', @report %> |
<%= link_to 'Back', reports_path %>
