<h1>Listing reports</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Query</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reports.each do |report| %>
  <tr>
    <td class='reptitle'><%= report.title %></td>
    <td><%= report.query[0..20] %></td>
    <td><%= link_to 'Show', report %></td>
    <!-- TODO: this is a form post -->
    <td><%= link_to 'Execute to csv', execute_report_path(report, :format => :csv) %></td>
    <td><%= link_to 'Execute to html', execute_report_path(report, :format => :html) %></td>
    <td><%= link_to 'Edit', edit_report_path(report) %></td>
    <td><%= link_to 'Destroy', report, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Report', new_report_path %>
